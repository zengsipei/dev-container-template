# Dev Container 环境变量配置
# 注意：此文件应加入 .gitignore

# ============================================
# 可选：WSL 用户目录
# ============================================
# 指向 WSL 中的用户主目录，用于绑定 AI agent 配置
# 如果不设置，将使用 named volume 存储配置

# 在 WSL 中使用（推荐）：
# WSL_HOME=/home/<username>

# 或使用 ~（会被展开）：
# WSL_HOME=~

# ============================================
# 说明
# ============================================
# 1. 有 WSL_HOME：bind mount 到指定目录（直接访问 WSL 文件）
# 2. 无 WSL_HOME：使用 named volume（数据在 Docker 管理区域）
# 3. compose.yaml 会自动处理，无需手动创建 volume
