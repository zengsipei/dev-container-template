# Dev Container 环境变量配置
# 注意：此文件应加入 .gitignore

# ============================================
# 可选：WSL 用户目录
# ============================================
# 指向 WSL 中的用户主目录，用于初始化 AI agent 配置
# 如果不设置，容器仍可正常启动，但需要手动配置 AI agents

# 在 WSL 中使用（推荐）：
# WSL_HOME=~

# 在 Windows 中使用：
# WSL_HOME=\\wsl.localhost\Ubuntu\home\dev

# ============================================
# 可选：API Keys（如果不用 WSL 中的配置）
# ============================================
# Anthropic API Key（用于 Claude Code）
# ANTHROPIC_API_KEY=your-key-here

# OpenAI API Key（用于 Codex）
# OPENAI_API_KEY=your-key-here

# Google API Key（用于 Gemini）
# GOOGLE_API_KEY=your-key-here

# GitHub Token（可选）
# GITHUB_TOKEN=your-token-here

# ============================================
# 说明
# ============================================
# 1. WSL_HOME 仅在首次启动时生效（volume 不存在时）
# 2. 有 WSL_HOME：创建 bind mount volume，直接指向该目录
# 3. 无 WSL_HOME：创建普通 named volume，数据在 Docker 管理区域
# 4. volume 创建后类型不会改变，切换需删除：docker volume rm dev-home
